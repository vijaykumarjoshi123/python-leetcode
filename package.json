{
  "name": "etlninja",
  "version": "1.0.0",
  "description": "ETLninja — the data-engineering skills platform (Python, SQL, Spark, dbt, Airflow, Kafka, Iceberg)",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
